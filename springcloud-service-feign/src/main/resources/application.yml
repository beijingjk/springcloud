eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
server:
  port: 8094
spring:
  application:
    name: service-feign
  servlet:
    multipart:
      max-file-size: 50Mb
      max-request-size: 100Mb

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html